# <windows-sample-host> Miloco full-ready validation report

Time: 2026-06-22 10:22:55 +08:00
Host: <windows-user>@<tailscale-ip>
Distro: Ubuntu-24.04
WSL user: <wsl-user>

Final validation:

```text
BASIC_READY=yes
FULL_READY=yes
PASS_COUNT=16
WARN_COUNT=0
FAIL_COUNT=0
```

Key evidence:

```text
Miloco service running=true
Miloco pid=12588
Miloco URL=http://127.0.0.1:1886
Miloco health={"status":"ok"}
OpenClaw gateway runtime=running
OpenClaw connectivity probe=ok
miloco-openclaw-plugin Status=loaded
account.is_bound=true
account.uid=250115363
account.nickname=mdidb
model.omni.api_key=configured
device_rows=127
camera.did=<camera-did-desk>
camera.name=<camera-desk>
camera.room=<room-bedroom>
camera.is_online=true
camera.in_use=true
camera.connected=true
recent_known_gap_logs=none
```

MiMo / Omni config:

```text
model.omni.model=mimo-v2.5
model.omni.base_url=https://token-plan-sgp.xiaomimimo.com/v1
model.omni.api_key=<MIMO_API_KEY>
```

Post-auth note:

```text
The first Finish validation ran while Miloco backend was still restarting and reported transient health failures.
After waiting, Miloco reached ready state, connected the target camera, called https://token-plan-sgp.xiaomimimo.com/v1/chat/completions successfully, and produced realtime_perceive output.
The validator was adjusted to avoid two false negatives:
1. Device list timeout was increased to 45s for large homes.
2. Historical pre-auth API-key log lines are no longer counted as current known-gap warnings.
```
